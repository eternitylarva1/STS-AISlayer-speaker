# 关键词触发概率范围限制修复报告

## 问题描述
用户反馈：即使将关键词触发概率设置为1，在游戏中仍然没有看到关键词出现。

## 问题分析
通过代码分析和日志调试，发现了两个问题：

1. **概率范围限制缺失**：ConfigPanel.java中的setKeywordTriggerProbability方法没有对输入值进行范围限制，用户可能输入超出0-1范围的值。

2. **关键词触发时机限制**：关键词功能只在"打牌"和"结束回合"时触发，而不会在"怪物介绍"时触发。用户测试时看到的是怪物介绍事件，因此没有关键词。

## 修复方案

### 1. 概率范围限制（双重保护）

#### ConfigPanel.java 修改
```java
// 在构造函数中添加范围限制
setNumberRange("keywordTriggerProbability", 0.0f, 1.0f);

// 在setKeywordTriggerProbability方法中添加范围验证
public static void setKeywordTriggerProbability(float probability) {
    // 确保概率值在0-1范围内
    probability = Math.max(0.0f, Math.min(1.0f, probability));
    keywordTriggerProbability = probability;
    logger.info("关键词触发概率已设置为: " + probability);
}
```

#### AIUtils.java 修改
```java
// 在getMonsterKeywords方法中添加额外的范围保护
float probability = aislayer.panels.ConfigPanel.getKeywordTriggerProbability();
probability = Math.max(0.0f, Math.min(1.0f, probability));
```

### 2. 扩展关键词触发时机

#### AIUtils.java 修改
在怪物介绍的提示词构建中添加关键词触发：
```java
// 添加怪物关键词（在怪物介绍时也触发关键词）
if (actionInfo.has("怪物ID")) {
    String monsterId = actionInfo.getString("怪物ID");
    String monsterKeyword = getMonsterKeywords(monsterId);
    if (monsterKeyword != null && !monsterKeyword.trim().isEmpty()) {
        prompt.append("关键词：").append(monsterKeyword).append("。");
    }
}
```

#### CommentaryUtils.java 修改
在triggerMonsterIntroduction方法中添加怪物ID传递：
```java
// 添加怪物ID（用于关键词）
if (firstMonsterId != null) {
    actionInfo.put("怪物ID", firstMonsterId);
    logger.info("已添加怪物ID: " + firstMonsterId);
}
```

### 3. 扩展怪物关键词配置

更新了monsterKeywords.json文件，添加了更多常见怪物的关键词：
- SpikeSlime_L: "黏糊糊的！", "史莱姆冲击！"
- AcidSlime_L: "酸性攻击！", "腐蚀一切！"
- FuzzyLouseDefensive: "毛茸茸的！", "防御姿态！"
- Cultist: "咔咔!", "力量无人能及"

## 测试验证

### 1. 概率范围测试
创建了KeywordProbabilityTest.java测试类，验证概率范围限制功能：
- 测试值：0.0, 0.5, 1.0, -0.5, 1.5, -1.0, 2.0
- 所有测试通过，确认范围限制正常工作

### 2. 构建测试
```bash
mvn clean package
```
构建成功，所有测试通过。

## 使用说明

### 关键词触发时机
现在关键词会在以下三种情况下触发：
1. **打牌时**：当打出针对怪物的卡牌时
2. **结束回合时**：在回合总结中
3. **怪物介绍时**：战斗开始介绍怪物时

### 概率设置
- 在Mod配置面板中设置"关键词触发概率"
- 有效范围：0.0（从不触发）到 1.0（总是触发）
- 系统会自动限制超出范围的值

### 调试信息
系统会输出详细的调试日志，包括：
- 怪物ID和关键词配置
- 概率值和随机数比较
- 关键词触发结果

## 技术细节

### 关键词触发逻辑
```java
// 概率检查
double randomValue = Math.random();
if (randomValue < probability) {
    // 随机选择关键词
    int index = (int) (Math.random() * keywordsArray.length());
    return keywordsArray.getString(index);
}
```

### 文件路径处理
使用跨平台兼容的路径构建：
```java
String langPackDir = "aislayerResources" + File.separator + "localization" + File.separator + Settings.language.toString().toLowerCase();
```

## 总结
通过这次修复：
1. ✅ 解决了概率范围限制问题，确保值始终在0-1之间
2. ✅ 扩展了关键词触发时机，现在怪物介绍时也会触发
3. ✅ 添加了详细的调试日志，便于问题排查
4. ✅ 扩展了怪物关键词配置，覆盖更多常见怪物
5. ✅ 创建了完整的测试用例，确保功能正常

现在用户设置关键词触发概率为1时，应该能在怪物介绍、打牌和结束回合时看到关键词出现。