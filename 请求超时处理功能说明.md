# 请求超时处理功能说明

## 功能描述
当AI请求超时时，系统会直接显示"请求超时"并跳过语音API调用，而不是显示"精彩的行动！"。

## 修改内容

### 1. 修改 callCommentaryAPI 方法

在 `src/main/java/aislayer/utils/AIUtils.java` 中修改了 `callCommentaryAPI` 方法，将所有错误情况下的返回值从 "精彩的行动！" 改为 "请求超时"：

```java
// 修改前
return "精彩的行动！";

// 修改后
return "请求超时";
```

### 2. 错误处理覆盖范围

现在在以下所有错误情况下都会返回"请求超时"：
- 构建解说请求失败
- 发送解说请求失败
- 解说API调用失败（非200响应码）
- Socket超时异常
- 其他异常情况

## 技术实现

### 错误处理逻辑
```java
private static String callCommentaryAPI(JSONObject actionInfo) throws IOException {
    // ... 其他代码 ...
    
    try {
        // 构建请求
    } catch (Exception e) {
        logger.error("构建解说请求失败", e);
        return "请求超时";  // 修改点1
    }
    
    // ... 发送请求 ...
    
    try {
        // 获取响应
        if (responseCode == 200) {
            // 正常处理
        } else {
            logger.error("解说API调用失败，响应码: " + responseCode);
            return "请求超时";  // 修改点2
        }
    } catch (SocketTimeoutException e) {
        logger.info("AI请求超时，耗时: " + requestDuration + "ms");
        return "请求超时";  // 修改点3
    } catch (Exception e) {
        logger.error("获取解说响应时发生错误", e);
        return "请求超时";  // 修改点4
    }
}
```

## 测试验证

### 构建测试
```bash
mvn clean package
```

构建成功，所有测试通过。

### 功能测试
现在当发生以下情况时，系统会显示"请求超时"：
1. API请求超时（SocketTimeoutException）
2. API返回非200状态码
3. 请求发送失败
4. 请求构建失败
5. 响应解析失败

## 使用说明

### 配置超时时间
在Mod配置面板中可以设置"解说请求超时时间"（单位：秒），默认值可以通过 `ConfigPanel.commentaryTimeout` 获取。

### 日志信息
系统会输出详细的错误日志，包括：
- 错误类型和堆栈跟踪
- 请求耗时信息
- 超时发生的时间点

## 技术特点

### 1. 统一错误处理
所有错误情况都返回相同的"请求超时"消息，确保用户体验一致性。

### 2. 详细的调试信息
保留完整的错误日志，便于问题排查和调试。

### 3. 超时时间配置
支持通过配置面板调整超时时间，适应不同的网络环境。

### 4. 跨平台兼容
使用标准的Java网络API和异常处理机制，确保在不同环境下都能正常工作。

## 注意事项

### 1. 语音API调用
当返回"请求超时"时，系统会跳过语音API调用，直接显示文本。

### 2. 错误恢复
系统会在下一次请求时自动重置，不会影响后续的AI解说功能。

### 3. 调试模式
在调试模式下，可以通过日志查看详细的错误信息，帮助定位问题。

## 总结

通过这次修改，系统在AI请求超时时会：
1. ✅ 显示"请求超时"而不是"精彩的行动！"
2. ✅ 跳过语音API调用
3. ✅ 保留详细的错误日志
4. ✅ 支持超时时间配置

现在用户可以更清楚地知道何时发生了请求超时，而不是看到模糊的"精彩的行动！"消息。