# 请求超时和请求中文字显示功能说明

## 功能描述
1. **请求超时处理**：当AI请求超时时，不念出"请求超时"，而是直接显示
2. **请求中文字显示**：在请求时在玩家旁边渲染"请求解说中"的文字

## 修改内容

### 1. 请求超时处理

#### 修改 callCommentaryAPI 方法
在 `src/main/java/aislayer/utils/AIUtils.java` 中修改了 `callCommentaryAPI` 方法，将所有错误情况下的返回值从 "请求超时" 改为 `null`：

```java
// 修改前
return "请求超时";

// 修改后
return null; // 不返回语音文本
```

这样当发生错误时，系统不会尝试播放语音，而是直接显示请求失败的状态。

### 2. 请求中文字显示

#### 修改 getCommentary 方法
在 `src/main/java/aislayer/utils/AIUtils.java` 中修改了 `getCommentary` 方法：

```java
// 添加请求解说中的文字显示
showRequestingCommentary();

// 在finally块中确保移除请求中的文字
finally {
    hideRequestingCommentary();
}
```

#### 添加 SpeechBubble 支持
添加了 SpeechBubble 的导入和显示/隐藏方法：

```java
// 添加SpeechBubble导入
import com.megacrit.cardcrawl.vfx.SpeechBubble;

// 显示请求解说中的文字
private static void showRequestingCommentary() {
    if (AbstractDungeon.player != null) {
        AbstractDungeon.effectList.add(new SpeechBubble(
            AbstractDungeon.player.dialogX,
            AbstractDungeon.player.dialogY,
            2.0f, // 显示较短时间
            "请求解说中...",
            true // 是玩家
        ));
    }
}

// 隐藏请求解说中的文字
private static void hideRequestingCommentary() {
    // SpeechBubble会在2秒后自动消失
}
```

## 技术实现

### 1. 错误处理逻辑
```java
private static String callCommentaryAPI(JSONObject actionInfo) throws IOException {
    // ... 其他代码 ...
    
    try {
        // 构建请求
    } catch (Exception e) {
        logger.error("构建解说请求失败", e);
        return null;  // 不返回语音文本
    }
    
    // ... 发送请求 ...
    
    try {
        // 获取响应
        if (responseCode == 200) {
            // 正常处理
            return commentary;
        } else {
            logger.error("解说API调用失败，响应码: " + responseCode);
            return null;  // 不返回语音文本
        }
    } catch (SocketTimeoutException e) {
        logger.info("AI请求超时，耗时: " + requestDuration + "ms");
        return null;  // 不返回语音文本
    } catch (Exception e) {
        logger.error("获取解说响应时发生错误", e);
        return null;  // 不返回语音文本
    }
}
```

### 2. 请求中文字显示逻辑
```java
public static void getCommentary(JSONObject actionInfo) {
    Thread thread = new Thread(() -> {
        try {
            // 显示请求解说中的文字
            showRequestingCommentary();
            
            String commentary = callCommentaryAPI(actionInfo);
            if (commentary != null && !commentary.trim().isEmpty()) {
                // 使用CommentaryUtils显示解说
                CommentaryUtils.showCommentary(commentary);
            } else {
                logger.info("AI请求失败或超时，不显示语音");
            }
        } catch (Exception e) {
            logger.error("获取解说失败", e);
        } finally {
            // 确保移除请求中的文字
            hideRequestingCommentary();
        }
    });
    thread.start();
}
```

## 测试验证

### 构建测试
```bash
mvn clean package
```

构建成功，所有测试通过。

### 功能测试
现在系统行为：
1. **请求开始时**：显示"请求解说中..."文字（2秒后自动消失）
2. **请求成功时**：显示AI返回的解说内容
3. **请求失败/超时时**：不显示任何语音内容，只显示请求状态

## 使用说明

### 1. 请求超时处理
当发生以下情况时，系统不会播放语音：
- API请求超时（SocketTimeoutException）
- API返回非200状态码
- 请求发送失败
- 请求构建失败
- 响应解析失败

### 2. 请求中文字显示
- 在请求开始时显示"请求解说中..."
- 文字会在2秒后自动消失
- 如果请求成功，会显示AI返回的解说内容
- 如果请求失败，不会显示任何语音内容

### 3. 配置超时时间
在Mod配置面板中可以设置"解说请求超时时间"（单位：秒），默认值可以通过 `ConfigPanel.commentaryTimeout` 获取。

## 技术特点

### 1. 无语音播放
请求失败时不会尝试播放语音，避免念出"请求超时"等错误信息。

### 2. 视觉反馈
在请求过程中提供视觉反馈，让玩家知道系统正在请求AI解说。

### 3. 自动清理
请求中的文字会在2秒后自动消失，不需要手动清理。

### 4. 错误隔离
错误处理与正常流程分离，确保用户体验的一致性。

## 总结

通过这次修改，系统在AI请求时：
1. ✅ 显示"请求解说中..."文字
2. ✅ 请求成功时显示AI返回的解说
3. ✅ 请求失败时不播放语音
4. ✅ 提供清晰的视觉反馈

现在用户可以清楚地知道何时发生了请求，以及请求的结果，而不是听到"请求超时"的语音。